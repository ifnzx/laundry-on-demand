# Local only — copy to .env and edit values
DATABASE_URL="file:./dev.db"
JWT_SECRET="ganti-dengan-string-rahasia-panjang-minimal-32-karakter"
NEXT_PUBLIC_APP_NAME="CleanStream Laundry"
PORT=3000
# SaaS fee per completed order (developer only — not editable by outlet admin)
PLATFORM_FEE_PER_ORDER=1500
